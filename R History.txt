setwd("~/Data Mining Final")
moviedata <- read.csv("movies.csv")
ratingsdata <- read.csv("movieratings.csv")
View(moviedata)
View(ratingsdata)
summary(moviedata)
summary(ratingsdata)
hist(moviedata$movieId)
hist(ratingsdata$movieId)
merge(moviedata, ratingsdata, c="movieId")
merged <- merge(moviedata, ratingsdata, c="movieId")
View(merged)
hist(merged$genres)
hist(merged$rating)
hist(ratingsdata$userId)
pie(table(merged$movieId))
pie(table(merged$rating))
barplot(table(merged$rating))